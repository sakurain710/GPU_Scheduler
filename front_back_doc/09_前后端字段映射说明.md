# 前后端字段映射说明

1. [认证与用户域](#认证与用户域)
2. [角色与权限域](#角色与权限域)
3. [GPU与任务域](#GPU与任务域)
4. [监控与运维域](#监控与运维域)

---

## 认证与用户域


## 1. 范围
- 后端来源：`AuthController`、`UserController`、`dto/auth/*`、`dto/user/*`、`dto/Result`。
- 前端范围：登录态、当前用户信息、用户管理列表与增删改查页面。

## 2. 通用响应壳映射（所有接口）

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `code` | `Result.code` | number | 是 | 业务状态码，成功为 `200` |
| `errorCode` | `Result.errorCode` | string | 否 | 失败时用于前端分支处理 |
| `message` | `Result.message` | string | 是 | 展示文案 |
| `data` | `Result.data` | object/array/null | 否 | 业务数据载荷 |


## 3. 认证模块字段映射

### 3.1 登录 `POST /api/auth/login`
请求映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 校验/示例 |
|---|---|---|---|---|
| `username` | `LoginRequest.username` | string | 是 | `@NotBlank`，示例 `alice` |
| `password` | `LoginRequest.password` | string | 是 | `@NotBlank`，示例 `P@ssw0rd` |


响应 `data` 映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `accessToken` | `LoginResponse.accessToken` | string | 是 | 访问令牌 |
| `refreshToken` | `LoginResponse.refreshToken` | string | 是 | 刷新令牌 |
| `tokenType` | `LoginResponse.tokenType` | string | 是 | 通常 `Bearer` |
| `expiresIn` | `LoginResponse.expiresIn` | number | 是 | accessToken 过期秒数 |
| `userId` | `LoginResponse.userId` | number | 是 | 当前用户 ID |
| `username` | `LoginResponse.username` | string | 是 | 当前用户名 |
| `roles` | `LoginResponse.roles` | string[] | 是 | 角色编码列表 |


### 3.2 刷新令牌 `POST /api/auth/refresh`
请求映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 校验 |
|---|---|---|---|---|
| `refreshToken` | `RefreshTokenRequest.refreshToken` | string | 是 | `@NotBlank` |


响应 `data`：与登录接口 `LoginResponse` 完全一致。

### 3.3 登出 `POST /api/auth/logout`
请求映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `refreshToken` | `LogoutRequest.refreshToken` | string | 否 | 传入则一并拉黑 refreshToken |
| `Authorization` | 请求头（`Bearer <accessToken>`） | string | 是 | 后端从请求头提取 accessToken |


响应 `data`：`null`。

### 3.4 当前用户 `GET /api/auth/me`
请求映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `Authorization` | 请求头（`Bearer <accessToken>`） | string | 是 | 登录态校验 |


响应 `data`（`UserResponse`）映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | `UserResponse.id` | number | 是 | 用户ID |
| `username` | `UserResponse.username` | string | 是 | 用户名 |
| `email` | `UserResponse.email` | string | 否 | 邮箱 |
| `status` | `UserResponse.status` | number | 是 | `0=禁用,1=启用` |
| `roles` | `UserResponse.roles` | string[] | 是 | 用户角色列表 |
| `createdAt` | `UserResponse.createdAt` | string | 否 | ISO-8601 时间 |
| `updatedAt` | `UserResponse.updatedAt` | string | 否 | ISO-8601 时间 |


## 4. 用户管理模块字段映射

### 4.1 创建用户 `POST /api/users`
请求（`CreateUserRequest`）：

| 前端字段 | 后端字段 | 类型 | 必填 | 校验/约束 |
|---|---|---|---|---|
| `username` | `CreateUserRequest.username` | string | 是 | `3-50`，`@NotBlank` |
| `password` | `CreateUserRequest.password` | string | 是 | `6-100`，`@NotBlank` |
| `email` | `CreateUserRequest.email` | string | 是 | `@Email`，`@NotBlank` |
| `status` | `CreateUserRequest.status` | number | 否 | 默认 `1` |


响应 `data`：`UserResponse`（结构同 3.4）。

### 4.2 更新用户 `PUT /api/users/{userId}`
路径参数：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `userId` | `@PathVariable userId` | number | 是 | 目标用户ID |


请求（`UpdateUserRequest`）：

| 前端字段 | 后端字段 | 类型 | 必填 | 校验 |
|---|---|---|---|---|
| `email` | `UpdateUserRequest.email` | string | 否 | `@Email` |
| `status` | `UpdateUserRequest.status` | number | 否 | `0/1` |


响应 `data`：`UserResponse`。

### 4.3 删除用户 `DELETE /api/users/{userId}`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `userId` | `@PathVariable userId` | number | 是 | 删除目标用户 |


响应 `data`：`null`。

### 4.4 用户详情 `GET /api/users/{userId}`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `userId` | `@PathVariable userId` | number | 是 | 查询目标用户 |


响应 `data`：`UserResponse`。

### 4.5 用户分页列表 `GET /api/users`
请求参数映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 默认值/约束 |
|---|---|---|---|---|
| `page` | `@RequestParam page` | number | 否 | 默认 `1`，最小 `1` |
| `size` | `@RequestParam size` | number | 否 | 默认 `10`，区间 `1-200` |
| `username` | `@RequestParam username` | string | 否 | 用户名模糊过滤 |
| `email` | `@RequestParam email` | string | 否 | 邮箱模糊过滤 |
| `status` | `@RequestParam status` | number | 否 | 状态过滤 |
| `sortBy` | `@RequestParam sortBy` | string | 否 | 默认 `createdAt`，可选 `createdAt/username/email/status/id` |
| `sortDir` | `@RequestParam sortDir` | string | 否 | 默认 `desc`，可选 `asc/desc` |


响应 `data`（`IPage<UserResponse>`）建议前端模型：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `records` | `IPage.records` | UserResponse[] | 是 | 当前页记录 |
| `total` | `IPage.total` | number | 是 | 总条数 |
| `size` | `IPage.size` | number | 是 | 每页大小 |
| `current` | `IPage.current` | number | 是 | 当前页码 |
| `pages` | `IPage.pages` | number | 是 | 总页数 |


### 4.6 分配用户角色 `POST /api/users/{userId}/roles`
路径/参数映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `userId` | `@PathVariable userId` | number | 是 | 目标用户ID |
| `roleIds` | `@RequestBody List<Long> roleIds` | number[] | 是 | 角色ID列表 |
| `expiresAt` | `@RequestParam expiresAt` | string(datetime) | 否 | 角色过期时间 |


响应 `data`：`null`。

## 5. 前端落地约定
- 本部分所有接口均使用统一响应壳，前端先判定 `code`，失败时再按 `errorCode` 分流。
- 登录后以 `tokenType + accessToken` 组装 `Authorization` 请求头。
- 用户状态当前以 `0/1` 为主，若未来扩展锁定态需以前端枚举可扩展方式实现。

---

## 角色与权限域


## 1. 范围
- 后端来源：`RoleController`、`RbacDictionaryController`、`dto/role/*`、`entity/Permission`、`entity/Resource`。
- 前端范围：角色管理页、角色授权弹窗、角色绑定用户弹窗、权限/资源字典下拉加载。

## 2. 角色管理字段映射（`/api/roles`）

### 2.1 创建角色 `POST /api/roles`
请求（`CreateRoleRequest`）映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 校验/默认 |
|---|---|---|---|---|
| `code` | `CreateRoleRequest.code` | string | 是 | `@NotBlank`，最大50 |
| `name` | `CreateRoleRequest.name` | string | 是 | `@NotBlank`，最大100 |
| `description` | `CreateRoleRequest.description` | string | 否 | 角色说明 |
| `roleType` | `CreateRoleRequest.roleType` | number | 否 | 默认 `1`，枚举：`1=System,2=Custom,3=Temporary` |
| `status` | `CreateRoleRequest.status` | number | 否 | 默认 `1`（启用） |
| `sortOrder` | `CreateRoleRequest.sortOrder` | number | 否 | 默认 `0` |
| `parentRoleId` | `CreateRoleRequest.parentRoleId` | number | 否 | 父角色ID |
| `permissionIds` | `CreateRoleRequest.permissionIds` | number[] | 否 | 初始权限ID列表 |


响应 `data`（`RoleResponse`）映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | `RoleResponse.id` | number | 是 | 角色ID |
| `code` | `RoleResponse.code` | string | 是 | 角色编码 |
| `name` | `RoleResponse.name` | string | 是 | 角色名称 |
| `description` | `RoleResponse.description` | string | 否 | 描述 |
| `roleType` | `RoleResponse.roleType` | number | 否 | 枚举：`1=System,2=Custom,3=Temporary` |
| `status` | `RoleResponse.status` | number | 是 | `0=禁用,1=启用` |
| `sortOrder` | `RoleResponse.sortOrder` | number | 否 | 排序值 |
| `parentRoleId` | `RoleResponse.parentRoleId` | number | 否 | 父角色ID |
| `permissionIds` | `RoleResponse.permissionIds` | number[] | 否 | 权限ID列表 |
| `createdAt` | `RoleResponse.createdAt` | string | 否 | ISO-8601 |
| `updatedAt` | `RoleResponse.updatedAt` | string | 否 | ISO-8601 |


### 2.2 更新角色 `PUT /api/roles/{roleId}`
路径参数：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `roleId` | `@PathVariable roleId` | number | 是 | 目标角色ID |


请求（`UpdateRoleRequest`）：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `name` | `UpdateRoleRequest.name` | string | 否 | 最大100 |
| `description` | `UpdateRoleRequest.description` | string | 否 | 角色说明 |
| `roleType` | `UpdateRoleRequest.roleType` | number | 否 | 枚举：`1=System,2=Custom,3=Temporary` |
| `status` | `UpdateRoleRequest.status` | number | 否 | `0/1` |
| `sortOrder` | `UpdateRoleRequest.sortOrder` | number | 否 | 排序值 |
| `parentRoleId` | `UpdateRoleRequest.parentRoleId` | number | 否 | 父角色ID |
| `permissionIds` | `UpdateRoleRequest.permissionIds` | number[] | 否 | 权限ID列表 |


响应 `data`：`RoleResponse`（结构同上）。

### 2.3 删除角色 `DELETE /api/roles/{roleId}`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `roleId` | `@PathVariable roleId` | number | 是 | 删除目标角色 |


响应 `data`：`null`。

### 2.4 角色详情 `GET /api/roles/{roleId}`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `roleId` | `@PathVariable roleId` | number | 是 | 查询目标角色 |


响应 `data`：`RoleResponse`。

### 2.5 角色列表 `GET /api/roles`
无查询参数。

响应 `data`：`RoleResponse[]`。

## 3. 角色授权/绑定字段映射

### 3.1 分配角色权限 `POST /api/roles/{roleId}/permissions`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `roleId` | 路径参数 `@PathVariable roleId` | number | 是 | 路径角色ID |
| `roleId` | `AssignPermissionsRequest.roleId` | number | 是 | 请求体角色ID（建议与路径一致） |
| `permissionIds` | `AssignPermissionsRequest.permissionIds` | number[] | 是 | 目标权限ID列表 |


响应 `data`：`null`。

### 3.2 撤销角色权限 `DELETE /api/roles/{roleId}/permissions`
请求体沿用 `AssignPermissionsRequest`，字段同 3.1。

### 3.3 分配角色用户 `POST /api/roles/{roleId}/users`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `roleId` | 路径参数 `@PathVariable roleId` | number | 是 | 路径角色ID |
| `roleId` | `AssignUsersRequest.roleId` | number | 是 | 请求体角色ID（建议与路径一致） |
| `userIds` | `AssignUsersRequest.userIds` | number[] | 是 | 用户ID列表 |
| `expiresAt` | `AssignUsersRequest.expiresAt` | string(datetime) | 否 | 绑定过期时间 |


响应 `data`：`null`。

### 3.4 解除角色用户绑定 `DELETE /api/roles/{roleId}/users`
请求体沿用 `AssignUsersRequest`；服务层使用 `roleId + userIds` 执行解绑。

## 4. RBAC 字典字段映射（`/api/rbac`）

### 4.1 权限字典 `GET /api/rbac/permissions`
请求参数：

| 前端字段 | 后端字段 | 类型 | 必填 | 默认值 |
|---|---|---|---|---|
| `resourceId` | `@RequestParam resourceId` | number | 否 | - |
| `status` | `@RequestParam status` | number | 否 | `1` |


响应 `data`：`Permission[]` 映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | `Permission.id` | number | 是 | 权限ID |
| `code` | `Permission.code` | string | 是 | 权限码 |
| `name` | `Permission.name` | string | 是 | 权限名 |
| `resourceId` | `Permission.resourceId` | number | 否 | 关联资源ID |
| `action` | `Permission.action` | string | 否 | `view/create/edit/delete/export` |
| `description` | `Permission.description` | string | 否 | 描述 |
| `status` | `Permission.status` | number | 是 | `0/1` |
| `createdAt` | `Permission.createdAt` | string | 否 | ISO-8601 |
| `updatedAt` | `Permission.updatedAt` | string | 否 | ISO-8601 |


### 4.2 资源字典 `GET /api/rbac/resources`
请求参数：

| 前端字段 | 后端字段 | 类型 | 必填 | 默认值 |
|---|---|---|---|---|
| `parentId` | `@RequestParam parentId` | number | 否 | - |
| `type` | `@RequestParam type` | number | 否 | - |
| `status` | `@RequestParam status` | number | 否 | `1` |


响应 `data`：`Resource[]` 映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | `Resource.id` | number | 是 | 资源ID |
| `code` | `Resource.code` | string | 是 | 资源编码 |
| `name` | `Resource.name` | string | 是 | 资源名称 |
| `type` | `Resource.type` | number | 是 | `1=菜单,2=API,3=按钮,4=数据` |
| `parentId` | `Resource.parentId` | number | 否 | 树形父节点ID |
| `path` | `Resource.path` | string | 否 | 路径或路由 |
| `sortOrder` | `Resource.sortOrder` | number | 否 | 排序值 |
| `description` | `Resource.description` | string | 否 | 描述 |
| `status` | `Resource.status` | number | 是 | `0/1` |
| `createdAt` | `Resource.createdAt` | string | 否 | ISO-8601 |
| `updatedAt` | `Resource.updatedAt` | string | 否 | ISO-8601 |


## 5. 前端实现约定
- 角色授权与角色绑人接口同时存在路径 `roleId` 与 body `roleId`，前端应保持一致，避免后端一致性校验失败。
- RBAC 字典建议在页面初始化阶段缓存，并按 `status=1` 作为默认查询条件。
- `permissionIds`、`userIds` 均为数字数组，提交前应去重并过滤空值。


---

## GPU与任务域


## 1. 范围
- 后端来源：`GpuController`、`GpuTaskController`、`dto/gpu/*`、`dto/task/*`。
- 前端范围：GPU资源管理、任务提交与我的任务、任务审批操作。

## 2. 路径映射约定

| 模块 | 路径前缀 | 说明 |
|---|---|---|
| GPU资源 | `/api/gpus` | 统一路径 |
| 任务管理 | `/api/tasks` | 统一路径 |

## 3. GPU 模块字段映射（`/api/gpus`）

### 3.1 GPU分页列表 `GET /api/gpus`
请求参数映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 默认值/约束 |
|---|---|---|---|---|
| `page` | `@RequestParam page` | number | 否 | 默认 `1`，最小 `1` |
| `size` | `@RequestParam size` | number | 否 | 默认 `20`，区间 `1-200` |
| `status` | `@RequestParam status` | number | 否 | `1=IDLE,2=BUSY,3=OFFLINE,4=MAINTENANCE` |
| `sortBy` | `@RequestParam sortBy` | string | 否 | 默认 `id`，可选 `id/name/memoryGb/computingPowerTflops/status/createdAt` |
| `sortDir` | `@RequestParam sortDir` | string | 否 | 默认 `asc`，可选 `asc/desc` |

响应 `data`（`IPage<GpuResponse>`）字段映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `records` | `IPage.records` | GpuResponse[] | 是 | 当前页GPU列表 |
| `total` | `IPage.total` | number | 是 | 总条数 |
| `size` | `IPage.size` | number | 是 | 每页大小 |
| `current` | `IPage.current` | number | 是 | 当前页 |
| `pages` | `IPage.pages` | number | 是 | 总页数 |

`GpuResponse` 映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | `GpuResponse.id` | number | 是 | GPU ID |
| `name` | `GpuResponse.name` | string | 是 | GPU型号名 |
| `manufacturer` | `GpuResponse.manufacturer` | string | 是 | 制造商 |
| `memoryGb` | `GpuResponse.memoryGb` | number | 是 | 显存(GB) |
| `computingPowerTflops` | `GpuResponse.computingPowerTflops` | number | 是 | 算力(TFLOPS) |
| `status` | `GpuResponse.status` | number | 是 | 状态码 `1/2/3/4` |
| `statusLabel` | `GpuResponse.statusLabel` | string | 否 | 状态文本 |
| `remark` | `GpuResponse.remark` | string | 否 | 备注 |
| `createdBy` | `GpuResponse.createdBy` | number | 否 | 创建人用户ID |
| `createdAt` | `GpuResponse.createdAt` | string | 否 | ISO-8601 |
| `updatedAt` | `GpuResponse.updatedAt` | string | 否 | ISO-8601 |

### 3.2 GPU详情 `GET /api/gpus/{gpuId}`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `gpuId` | `@PathVariable gpuId` | number | 是 | GPU主键 |

响应 `data`：`GpuResponse`。

### 3.3 注册GPU `POST /api/gpus`
请求（`RegisterGpuRequest`）映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 校验/约束 |
|---|---|---|---|---|
| `name` | `RegisterGpuRequest.name` | string | 是 | `@NotBlank`，最大128 |
| `manufacturer` | `RegisterGpuRequest.manufacturer` | string | 是 | `@NotBlank`，最大64 |
| `memoryGb` | `RegisterGpuRequest.memoryGb` | number | 是 | `> 0` |
| `computingPowerTflops` | `RegisterGpuRequest.computingPowerTflops` | number | 是 | `> 0` |
| `remark` | `RegisterGpuRequest.remark` | string | 否 | 最大255 |

响应 `data`：`GpuResponse`。

### 3.4 更新GPU状态 `PUT /api/gpus/{gpuId}/status`

| 前端字段 | 后端字段 | 类型 | 必填 | 约束 |
|---|---|---|---|---|
| `gpuId` | `@PathVariable gpuId` | number | 是 | 目标GPU |
| `status` | `UpdateGpuStatusRequest.status` | number | 是 | `1-4` |

响应 `data`：`GpuResponse`。

### 3.5 删除GPU `DELETE /api/gpus/{gpuId}`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `gpuId` | `@PathVariable gpuId` | number | 是 | 删除目标GPU |

响应 `data`：`null`。

### 3.6 GPU健康摘要 `GET /api/gpus/health`
响应 `data`：`Map<String, Long>`（键值由服务端定义，前端按 key/value 动态渲染）。

### 3.7 GPU利用率指标 `GET /api/gpus/metrics`
响应 `data`：`Map<String, Object>`（指标模型动态，前端需容错字段缺失）。

### 3.8 Worker心跳上报 `POST /api/gpus/{gpuId}/heartbeat`
响应 `data` 字段：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `gpuId` | `data.gpuId` | number | 是 | GPU ID |
| `accepted` | `data.accepted` | boolean | 是 | 是否接收 |
| `heartbeatAgeSeconds` | `data.heartbeatAgeSeconds` | number | 是 | 心跳年龄（秒） |

## 4. 任务模块字段映射（`/api/tasks`）

### 4.1 提交任务 `POST /api/tasks/submit`
请求（`SubmitTaskRequest`）映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 校验/示例 |
|---|---|---|---|---|
| `title` | `SubmitTaskRequest.title` | string | 是 | `@NotBlank`，最大128 |
| `description` | `SubmitTaskRequest.description` | string | 否 | 最大65535 |
| `taskType` | `SubmitTaskRequest.taskType` | string | 是 | `@NotBlank`，示例 `model_training` |
| `minMemoryGb` | `SubmitTaskRequest.minMemoryGb` | number | 是 | `> 0` |
| `computeUnitsGflop` | `SubmitTaskRequest.computeUnitsGflop` | number | 是 | `> 0` |
| `basePriority` | `SubmitTaskRequest.basePriority` | number | 否 | 默认 `5`，区间 `1-10` |

响应 `data`：`TaskResponse`（见 4.6）。

### 4.2 任务详情 `GET /api/tasks/{taskId}`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `taskId` | `@PathVariable taskId` | number | 是 | 任务ID |

响应 `data`：`TaskResponse`。

### 4.3 我的任务列表 `GET /api/tasks/my`
请求参数映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 默认值/约束 |
|---|---|---|---|---|
| `page` | `@RequestParam page` | number | 否 | 默认 `1`，最小 `1` |
| `size` | `@RequestParam size` | number | 否 | 默认 `10`，区间 `1-200` |
| `status` | `@RequestParam status` | number | 否 | 任务状态过滤 |
| `sortBy` | `@RequestParam sortBy` | string | 否 | 默认 `createdAt`，可选 `createdAt/basePriority/enqueueAt/status/id` |
| `sortDir` | `@RequestParam sortDir` | string | 否 | 默认 `desc` |

响应 `data`：`IPage<TaskResponse>`（分页字段同 3.1）。

### 4.4 取消任务 `POST /api/tasks/{taskId}/cancel`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `taskId` | `@PathVariable taskId` | number | 是 | 任务ID |

响应 `data`：`null`。

### 4.5 待审批任务列表 `GET /api/tasks/approval/pending`

| 前端字段 | 后端字段 | 类型 | 必填 | 默认值/约束 |
|---|---|---|---|---|
| `page` | `@RequestParam page` | number | 否 | 默认 `1`，最小 `1` |
| `size` | `@RequestParam size` | number | 否 | 默认 `10`，区间 `1-200` |
| `sortBy` | `@RequestParam sortBy` | string | 否 | 默认 `createdAt` |
| `sortDir` | `@RequestParam sortDir` | string | 否 | 默认 `asc` |

响应 `data`：`IPage<TaskResponse>`。

### 4.6 审批通过/拒绝

`POST /api/tasks/{taskId}/approve`：无请求体，返回 `TaskResponse`。  
`POST /api/tasks/{taskId}/reject`：请求体 `RejectTaskRequest`。

### 4.7 批量审批

`POST /api/tasks/approval/batch/approve`：请求体 `BatchApproveRequest`，返回 `TaskResponse[]`。  
`POST /api/tasks/approval/batch/reject`：请求体 `BatchApproveRequest`，返回 `TaskResponse[]`。

`BatchApproveRequest` 映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `taskIds` | `BatchApproveRequest.taskIds` | number[] | 是 | 任务ID列表，非空且元素>0，最多100个 |
| `reason` | `BatchApproveRequest.reason` | string | 否 | 批量拒绝原因 |

`RejectTaskRequest` 映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `reason` | `RejectTaskRequest.reason` | string | 否 | 拒绝原因 |

`TaskResponse` 映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | `TaskResponse.id` | number | 是 | 任务ID |
| `userId` | `TaskResponse.userId` | number | 是 | 提交人ID |
| `gpuId` | `TaskResponse.gpuId` | number/null | 否 | 分配GPU ID |
| `title` | `TaskResponse.title` | string | 是 | 标题 |
| `description` | `TaskResponse.description` | string | 否 | 描述 |
| `taskType` | `TaskResponse.taskType` | string | 是 | 任务类型 |
| `minMemoryGb` | `TaskResponse.minMemoryGb` | number | 是 | 最低显存需求 |
| `computeUnitsGflop` | `TaskResponse.computeUnitsGflop` | number | 是 | 计算量估值 |
| `basePriority` | `TaskResponse.basePriority` | number | 是 | 基础优先级 |
| `status` | `TaskResponse.status` | number | 是 | 状态码 |
| `statusLabel` | `TaskResponse.statusLabel` | string | 否 | 状态文本 |
| `estimatedSeconds` | `TaskResponse.estimatedSeconds` | number | 否 | 预估执行时长 |
| `actualSeconds` | `TaskResponse.actualSeconds` | number | 否 | 实际执行时长 |
| `errorMessage` | `TaskResponse.errorMessage` | string | 否 | 失败原因 |
| `enqueueAt` | `TaskResponse.enqueueAt` | string | 否 | 入队时间 |
| `dispatchedAt` | `TaskResponse.dispatchedAt` | string | 否 | 调度时间 |
| `estimatedFinishAt` | `TaskResponse.estimatedFinishAt` | string | 否 | 预计完成时间 |
| `finishedAt` | `TaskResponse.finishedAt` | string | 否 | 完成时间 |
| `createdAt` | `TaskResponse.createdAt` | string | 否 | 创建时间 |

## 5. 前端实现约定
- `GpuResponse.status` 与 `TaskResponse.status` 建议在前端统一维护枚举字典，`statusLabel` 仅用于兜底显示。
- `metrics/health` 响应为动态键值结构，前端应做 `key in data` 判断，避免字段强耦合。

---

## 监控与运维域


## 1. 范围
- 后端来源：`MonitoringController`、`OpsController`、`dto/monitor/*`、`TaskStatusNotification`、`WebSocketConfig`。
- 前端范围：监控大盘、运维控制台、实时订阅消息处理。

## 2. 监控接口字段映射（`/api`）

### 2.1 系统健康 `GET /api/health`
响应 `data`（`SystemHealth`）映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `status` | `SystemHealth.status` | string | 是 | `UP / DEGRADED / DOWN` |
| `dbStatus` | `SystemHealth.dbStatus` | string | 是 | MySQL连通状态 |
| `redisStatus` | `SystemHealth.redisStatus` | string | 是 | Redis连通状态 |
| `schedulerStatus` | `SystemHealth.schedulerStatus` | string | 是 | 调度器状态 |
| `circuitBreakerState` | `SystemHealth.circuitBreakerState` | string | 是 | 熔断器状态 |
| `oldestQueuedTaskSeconds` | `SystemHealth.oldestQueuedTaskSeconds` | number | 是 | 队列最老任务等待秒数，`-1` 代表队列为空 |

### 2.2 合并指标 `GET /api/metrics`
响应 `data`（`MetricsResponse`）结构：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `tasks` | `MetricsResponse.tasks` | object | 是 | 任务指标对象（`TaskMetrics`） |
| `gpus` | `MetricsResponse.gpus` | object | 是 | GPU指标对象（`GpuMetrics`） |

`tasks`（`TaskMetrics`）映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `queueLength` | `TaskMetrics.queueLength` | number | 是 | 队列总长度 |
| `queueLengthByPriority` | `TaskMetrics.queueLengthByPriority` | Record<string, number> | 否 | 按优先级分桶 |
| `taskCountByStatus` | `TaskMetrics.taskCountByStatus` | Record<string, number> | 否 | 按状态统计 |
| `avgWaitSecondsByPriority` | `TaskMetrics.avgWaitSecondsByPriority` | Record<string, number> | 否 | 各优先级平均等待 |
| `avgDispatchLatencySeconds` | `TaskMetrics.avgDispatchLatencySeconds` | number | 否 | 平均调度延迟 |
| `avgTurnaroundSeconds` | `TaskMetrics.avgTurnaroundSeconds` | number | 否 | 平均周转时长 |
| `dispatchLatencyPercentilesSeconds` | `TaskMetrics.dispatchLatencyPercentilesSeconds` | Record<string, number> | 否 | 延迟分位值 |
| `queueAgeHistogram` | `TaskMetrics.queueAgeHistogram` | Record<string, number> | 否 | 队龄分布 |
| `completionRate` | `TaskMetrics.completionRate` | string | 否 | 完成率（字符串百分比） |
| `failureRate` | `TaskMetrics.failureRate` | string | 否 | 失败率（字符串百分比） |
| `failureReasons` | `TaskMetrics.failureReasons` | Record<string, number> | 否 | 失败原因分布 |
| `allocationFailureReasons` | `TaskMetrics.allocationFailureReasons` | Record<string, number> | 否 | 分配失败原因 |
| `userSlaCompliancePct` | `TaskMetrics.userSlaCompliancePct` | Record<string, number> | 否 | 用户SLA达标率 |
| `retryQueueSize` | `TaskMetrics.retryQueueSize` | number | 否 | 重试队列大小 |
| `dlqSize` | `TaskMetrics.dlqSize` | number | 否 | 死信队列大小 |
| `pendingApprovalCount` | `TaskMetrics.pendingApprovalCount` | number | 否 | 待审批任务数 |
| `webhookRetryQueueSize` | `TaskMetrics.webhookRetryQueueSize` | number | 否 | webhook重试队列大小 |

`gpus`（`GpuMetrics`）映射：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `total` | `GpuMetrics.total` | number | 是 | GPU总数 |
| `countByStatus` | `GpuMetrics.countByStatus` | Record<string, number> | 否 | 各状态GPU数量 |
| `utilizationRate` | `GpuMetrics.utilizationRate` | string | 否 | 整体利用率（字符串百分比） |
| `usedMemoryGbByGpu` | `GpuMetrics.usedMemoryGbByGpu` | Record<string, number> | 否 | 每卡已用显存 |
| `remainingMemoryGbByGpu` | `GpuMetrics.remainingMemoryGbByGpu` | Record<string, number> | 否 | 每卡剩余显存 |
| `vramFragmentationByGpu` | `GpuMetrics.vramFragmentationByGpu` | Record<string, number> | 否 | 显存碎片率 |
| `idleSecondsByGpu` | `GpuMetrics.idleSecondsByGpu` | Record<string, number> | 否 | 空闲时长秒数 |

## 3. 运维接口字段映射（`/api/ops`）

### 3.1 调度器控制

| 接口 | 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|---|
| `POST /dispatcher/pause` | `paused` | `data.paused` | boolean | 是 | 固定返回 `true` |
| `POST /dispatcher/resume` | `paused` | `data.paused` | boolean | 是 | 固定返回 `false` |
| `GET /dispatcher/status` | `paused` | `data.paused` | boolean | 是 | 当前调度暂停状态 |

### 3.2 熔断器与死信队列

`POST /circuit-breaker/reset` 响应：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `state` | `data.state` | string | 是 | 熔断器状态名 |
| `failureCount` | `data.failureCount` | number | 是 | 当前失败计数 |

`GET /dlq` 响应：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `size` | `data.size` | number | 是 | 死信队列数量 |
| `items` | `data.items` | array | 是 | 死信任务条目列表（动态结构） |

`POST /dlq/clear` 响应：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `removed` | `data.removed` | number | 是 | 清理条数 |

`POST /dlq/{taskId}/reprocess` 响应：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `taskId` | `data.taskId` | number | 是 | 重处理任务ID |
| `reprocessed` | `data.reprocessed` | boolean | 是 | 是否重处理成功 |

### 3.3 队列与任务强制操作

`POST /queue/drain` 请求体：`RejectTaskRequest.reason`（可选）。

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `reason` | `RejectTaskRequest.reason` | string | 否 | 排空队列原因 |

`POST /queue/drain` 响应：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `drained` | `data.drained` | number | 是 | 排空任务数 |
| `reason` | `data.reason` | string | 是 | 实际使用原因 |

`POST /tasks/{taskId}/preempt` 响应：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `taskId` | `data.taskId` | number | 是 | 被抢占任务ID |
| `preempted` | `data.preempted` | boolean | 是 | 固定 `true` |

`POST /tasks/{taskId}/force-requeue`、`POST /tasks/{taskId}/force-fail`：响应 `data` 均为 `null`。

### 3.4 通知重试队列状态
`GET /notification/webhook/retry/status` 响应：

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `queueSize` | `data.queueSize` | number | 是 | webhook重试队列长度 |

## 4. 实时消息字段映射（WebSocket/STOMP）

### 4.1 连接与订阅

| 前端概念 | 后端配置 | 说明 |
|---|---|---|
| SockJS连接端点 | `/ws` | `WebSocketConfig.registerStompEndpoints` |
| 消息代理前缀 | `/topic`、`/queue` | `WebSocketConfig.configureMessageBroker` |
| 遥测主题 | `/topic/telemetry` | 默认值，可通过 `telemetry.topic` 配置覆盖 |
| 任务状态主题 | `/topic/task-status/{userId}` | `notification.task-topic-prefix` + `userId` |

### 4.2 遥测消息体（`TelemetrySnapshot`）

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `timestamp` | `TelemetrySnapshot.timestamp` | string | 是 | 快照时间 |
| `health` | `TelemetrySnapshot.health` | object | 是 | `SystemHealth` |
| `tasks` | `TelemetrySnapshot.tasks` | object | 是 | `TaskMetrics` |
| `gpus` | `TelemetrySnapshot.gpus` | object | 是 | `GpuMetrics` |

### 4.3 任务状态消息体（`TaskStatusNotification`）

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `taskId` | `TaskStatusNotification.taskId` | number | 是 | 任务ID |
| `userId` | `TaskStatusNotification.userId` | number | 是 | 归属用户ID |
| `fromStatus` | `TaskStatusNotification.fromStatus` | string | 否 | 旧状态文本 |
| `toStatus` | `TaskStatusNotification.toStatus` | string | 是 | 新状态文本 |
| `occurredAt` | `TaskStatusNotification.occurredAt` | string | 是 | 发生时间 |
| `message` | `TaskStatusNotification.message` | string | 否 | 错误/拒绝原因 |

## 5. 前端实现约定
- 监控接口与运维接口均走统一响应壳，先判 `code`，再按 `errorCode` 做异常分流。
- 监控指标中 `Map` 类型字段建议按“动态 key-value”渲染，不要写死完整键名集合。
- STOMP订阅建议在登录成功后以 `userId` 拼接个人任务主题，并在登出时主动取消订阅。

---

