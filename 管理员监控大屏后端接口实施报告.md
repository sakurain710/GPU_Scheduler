# 管理员监控大屏后端接口实施报告

## 1. 目标
- 为管理员 dashboard 提供首屏实时快照、排队耗时趋势、死信队列结构化列表。
- 页面采用“REST 首屏 + WebSocket 增量 + REST 回补”模式。
- 严格不进行任何 MySQL 表结构变更。

## 2. 新增接口

### 2.1 `GET /api/admin/dashboard/initial`
- 用途：页面首次加载时获取实时块。
- 返回：
  - `overview`
  - `memoryFragmentation`
  - `dlqSummary`
  - `lastTelemetryAt`

### 2.2 `GET /api/admin/dashboard/queue-wait-trend`
- 参数：
  - `mode=DAY|WEEK`
  - `anchorDate=yyyy-MM-dd`
- 用途：获取指定日 24 小时趋势或指定周 7 天趋势。
- 返回每个 bucket 的：
  - `actualAgingAvgWaitSeconds`
  - `simulatedFifoAvgWaitSeconds`

### 2.3 `GET /api/admin/dashboard/dlq`
- 参数：
  - `page`
  - `size`
- 用途：分页获取结构化死信列表。
- 返回字段：
  - `taskId`
  - `username`
  - `email`
  - `retryCount`
  - `failureReason`
  - `enteredDlqAt`

## 3. WebSocket
- 新增 topic：
  - `/topic/admin/dashboard/telemetry`
- 推送内容：
  - `timestamp`
  - `overview`
  - `memoryFragmentation`
  - `dlqSummary`
- 不推送：
  - 24 小时 / 7 天趋势图
  - DLQ 分页列表

## 4. 数据来源与计算规则

### 4.1 系统健康
- MySQL 连通性：复用现有数据库查询探针。
- MySQL QPS：以应用侧执行计数差分估算。
- Redis 连通性：复用现有连接探针。
- Redis 碎片率：读取 `INFO memory` 的 `mem_fragmentation_ratio`。
- 调度线程池状态：来自 `TaskExecutionSimulator` 暴露的线程池指标。
- 熔断器状态：复用 `CircuitBreakerService`。

### 4.2 Best Fit 显存碎片
- `usedAllocatedMemoryGb`：运行中任务 `minMemoryGb` 总和。
- `fragmentedMemoryGb`：`BUSY` GPU 剩余显存总和。
- `freeMemoryGb`：`IDLE` GPU 全部可分配显存总和。
- 同时返回每张 GPU 的三段拆分明细。

### 4.3 排队耗时趋势
- 不新增任何 `monitor_*` 表。
- 真实 Aging 线：
  - 取 `gpu_task.enqueue_at -> dispatched_at` 的真实排队时长
  - 按 `enqueue_at` 所在小时/天分桶聚合
- FIFO 基线：
  - 使用同一时间窗内真实任务样本
  - 按 `enqueue_at` 升序离线重放
  - 不启用 aging
  - 使用真实资源需求与真实 GPU 释放时间近似资源回收
- `DAY` 固定返回 24 个点
- `WEEK` 固定返回 7 个点
- 空桶补零
- 结果写入 Redis 短期缓存，默认 TTL 300 秒

### 4.4 DLQ 结构化列表
- 底层仍使用现有 Redis List `gpu:task:dlq`
- 兼容现有 JSON payload：
  - `taskId`
  - `attempt`
  - `reason`
  - `time`
- 查询时联查现有 `gpu_task` 和 `user`，不改 MySQL

## 5. 页面协同策略
- 首屏：
  - 调 `GET /api/admin/dashboard/initial`
  - 调 `GET /api/admin/dashboard/queue-wait-trend`
  - 调 `GET /api/admin/dashboard/dlq`
- 增量：
  - 订阅 `/topic/admin/dashboard/telemetry`
- 回补：
  - WebSocket 断开后定时重新拉取 `initial`
  - 趋势图在切换模式、切换日期/周、或定时校准时重新拉取
  - DLQ 列表在摘要变化或操作后重新拉取

## 6. 权限
- 新接口使用：
  - `ROLE_ADMIN`
  - 或 `monitoring:read`

## 7. 测试与验收
- 单元测试覆盖：
  - 显存碎片统计
  - DLQ 结构化列表
  - 24 小时趋势固定长度
  - 管理员大屏控制器
  - 管理员 telemetry 推送
- 回归要求：
  - 旧 `/api/health`
  - 旧 `/api/metrics`
  - 旧 `/topic/telemetry`
  行为保持不变

## 8. 约束声明
- 本实现未新增、删除或修改任何 MySQL 表、字段、索引、约束。
- 所有新增能力均基于现有业务表、现有 Redis、现有 WebSocket 架构完成。
