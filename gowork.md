
# 前后端对接文档生成 Prompt（正式版）

## 🎯 任务目标

基于当前 **Spring Boot 后端项目真实代码**，完成：

1. 判断项目是否达到“可对接前端开发”的标准
2. 若未达到，进行最小必要补充
3. 制定对接文档编写计划（`to_front_plan.md`）
4. 分步骤生成完整前后端对接文档（存放于 `front_back_doc/` 目录）

---

## ⚠️ 全局强约束（必须遵守）

### 1. 禁止臆造

* 禁止基于常见项目经验假设接口、字段、流程、权限模型、图表结构
* 凡代码中不存在 / 配置中未体现 / 注释中未说明的内容：

    * ❌ 不得写成既定事实
    * ✅ 必须标记为：

        * `待确认`
        * 或 `当前后端暂未实现`

---

### 2. 必须基于真实代码

所有输出内容必须来源于：
* com.sakurain.** 包下的代码
* docs/mysql/gpu_scheduler_db/**中的 SQL 文件
* 项目根目录下的配置文件（如 application.yml、env/env等）

---

### 3. 先审查，后生成

必须严格按阶段执行：

```
禁止：
- 直接写文档
- 直接生成模板
- 未分析代码就输出内容
```

---

### 4. 最小改动原则（如需修改后端）

若后端未达标：

* 先列差距清单
* 再决定是否修改
* 修改必须：

    * 最小范围
    * 不破坏现有结构
    * 不随意重构
    * 不改变已有接口语义

---

### 5. 文档真实性优先

若后端不支持某功能：

* ❌ 不允许“为了完整性补写文档”
* ✅ 必须明确写：

    * 当前不支持
    * 或需后端补充

---

### 6. 文档语言要求

* 默认使用：**简体中文**
* 技术字段：保留英文命名

---

---

# 🧩 第一阶段：项目审查

请先完整扫描项目，并从以下维度进行判断：

## 审查维度

### 1. 接口能力

* Controller 是否完整
* 是否具备 CRUD / 分页 / 查询能力
* 是否具备前端常用接口结构

### 2. 响应规范

* 是否统一返回结构（如 code / msg / data）
* 是否统一错误码

### 3. 数据结构

* DTO / VO / Entity 是否清晰
* 字段是否稳定、命名是否规范

### 4. 权限体系

* 是否存在认证机制（JWT / Session / Token）
* 是否存在接口权限控制
* 是否存在按钮级 / 资源级权限

### 5. 业务支撑能力

* 是否支持页面基本流程（列表 / 详情 / 编辑等）
* 是否具备业务状态流转支持

### 6. 图表支持能力（重点）

是否具备用于以下场景的数据接口：

> ⚠️ 用于 ECharts 实现
> **“显卡资源池状态的动态可视化监控”**

---

### 7. 文档支撑能力

* 是否有 Swagger / OpenAPI
* 是否有注释可用于生成接口说明

---

### 8. 工程规范风险

* 字段不统一
* 返回结构混乱
* 命名不一致
* 接口风格不统一

---

## 📌 输出格式（必须使用）

```markdown
## 审查结果

### ✅ 已满足项
- ...

### ❌ 未满足项
- ...

### ⚠️ 风险项
- ...

### 📊 结论
是否达到“可对接前端标准”：是 / 否
```

---

---

# 🔧 第二阶段：差距分析与处理

仅在“不满足标准”时执行。

## 要求

### 1. 输出差距清单

每一项必须说明：

* 问题描述
* 影响的前端能力（页面 / 接口 / 图表）
* 是否必须修复

---

### 2. 分类处理

| 类型   | 处理方式        |
| ---- | ----------- |
| 必须修复 | 可进行最小代码补充   |
| 可延后  | 文档中标注“暂不支持” |
| 不确定  | 标记“待确认”     |

---

### 3. 修改规则（如执行）

* 只补充必要能力
* 不做架构重构
* 不破坏已有接口
* 修改后需同步影响文档

---

---

# 🗂️ 第三阶段：制定文档计划

生成文件：

```
to_front_plan.md
```

---

## 必须包含内容

### 1. 文档清单

```text
1. 项目概述
2. 接口文档
3. 数据字典
4. 业务流程文档
5. 页面功能结构与交互说明（最重要）
6. 权限设计说明
7. 统一响应与错误码规范
8. 联调说明文档
9. 前后端字段映射说明
10. ECharts 图表接口说明
```

---

### 2. 每份文档必须说明：

* 目标
* 来源代码（Controller / Service / Entity 等）
* 编写顺序
* 是否需要拆分执行

---

### 3. 必须包含

* 风险项
* 待确认项

---

---

# 📝 第四阶段：生成对接文档

## 📁 输出目录

```
front_back_doc/
```

---

## 📄 文档清单（必须全部完成）

### 1. 项目概述

* 系统定位
* 模块划分
* 技术架构
* 数据流说明

---

### 2. 接口文档（字段级）

每个接口必须包含：

* 接口路径
* 请求方式
* 请求头
* 请求参数（字段级）
* 响应字段（字段级）
* 枚举值说明
* 是否可空
* 默认值
* 示例（成功 / 失败）

---

### 3. 数据字典

* 所有核心字段
* 枚举值
* 状态字段
* 时间/金额格式规则

---

### 4. 业务流程文档

说明：

* 接口调用顺序
* 页面与接口关系
* 状态流转

---

### 5. 页面功能结构与交互说明（最重要）

必须包含：

* 页面结构（功能区域）
* 展示数据来源
* 操作行为
* 状态变化
* 接口依赖关系

---

### ❗禁止内容：

* UI设计
* 配色
* 动效
* 视觉描述

---

### 6. 权限设计说明

* 登录认证方式
* Token 使用方式
* 接口权限
* 菜单权限
* 按钮权限
* 数据权限

---

### 7. 统一响应与错误码规范

* 标准响应结构
* 错误码定义
* 前端处理方式

---

### 8. 联调说明文档

必须包含：

* API Base URL
* Swagger 地址
* 测试账号
* 角色账号
* 跨域说明
* 接口完成状态

---

### 9. 前后端字段映射说明

| 含义 | 后端字段 | 前端字段 | 备注 |
| -- | ---- | ---- | -- |

---

### 10. ECharts 图表接口说明（重点）

必须说明：

* 图表类型
* 数据结构
* X/Y轴字段
* 单位
* 多系列结构
* 是否需要前端转换

---

---

# 📏 文档编写规范

## 1. 必须字段级说明

接口与数据结构必须细化到字段级

---

## 2. 大文档拆分

内容较大时：

* 必须分步骤生成
* 避免上下文丢失

---

## 3. 标注来源（强烈建议）

示例：

```text
依据：UserController.java
依据：OrderServiceImpl.java
```

---

## 4. 状态标记（必须）

所有能力必须标记：

* ✅ 已实现
* ❌ 未实现
* ⚠️ 待确认

---

## 5. 严格一致性

* 文档 ≠ 推测
* 文档 = 代码映射

---

---

# ✅ 最终目标

输出结果必须满足：

* 可直接供前端开发使用
* 无歧义
* 无字段不确定性
* 无接口理解偏差
* 可支持 Vue + Element UI + ECharts 开发

---

如果执行过程中发现：

* 后端能力无法支撑前端实现
* 或设计存在重大缺陷

必须**优先提出问题**，而不是继续生成错误文档。
