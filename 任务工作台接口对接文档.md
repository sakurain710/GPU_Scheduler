# 任务工作台接口对接文档

1. [文档范围](#文档范围)
2. [接口总览](#接口总览)
3. [HTTP 接口](#http-接口)
4. [WebSocket 实时订阅](#websocket-实时订阅)
5. [DTO 字段映射](#dto-字段映射)
6. [前端对接建议](#前端对接建议)
7. [后端实现依据](#后端实现依据)

---

## 文档范围

- 后端来源：
  - `GpuTaskController`
  - `TaskDashboardService`
  - `TaskDashboardPushService`
  - `TaskNotificationService`
  - `WebSocketConfig`
  - `dto/task/*`
- 前端范围：
  - 任务工作台页面首屏加载
  - 用户任务卡片/列表渲染
  - GPU 资源雷达渲染
  - 优先队列 Top5 渲染
  - 实时快照订阅与页面刷新

---

## 接口总览

### 1. 首屏 HTTP

| 能力 | 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|---|
| 任务工作台首屏 | `GET` | `/api/tasks/dashboard` | Bearer Token | 返回当前用户工作台完整快照 |

### 2. 实时订阅

| 能力 | 类型 | 端点/主题 | 鉴权 | 说明 |
|---|---|---|---|---|
| WebSocket 连接 | `WS/STOMP` | `/ws` | 登录态 | 原生 WebSocket STOMP 与 SockJS STOMP 共用端点 |
| 工作台实时快照 | `TOPIC` | `/topic/task-dashboard/{userId}` | 登录态 | 返回 `TaskDashboardResponse` |

### 3. 与旧主题关系

- 页面主数据源：
  - `GET /api/tasks/dashboard`
  - `/topic/task-dashboard/{userId}`
- 仍可保留但不是本页主数据源：
  - `/topic/task-status/{userId}`
- 本页不需要再额外拼接：
  - `/api/metrics`
  - `/topic/telemetry`

---

## HTTP 接口

## 1. 获取任务工作台 `GET /api/tasks/dashboard`

### 1.1 请求参数

| 参数 | 后端字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|---|
| `page` | `@RequestParam page` | number | 否 | `1` | 工作台内任务列表页码 |
| `size` | `@RequestParam size` | number | 否 | `10` | 工作台内任务列表每页条数，范围 `1-200` |
| `status` | `@RequestParam status` | number | 否 | `null` | 当前用户任务状态过滤 |
| `sortBy` | `@RequestParam sortBy` | string | 否 | `updatedAt` | 列表排序字段，支持 `updatedAt/basePriority/enqueueAt/status/id/createdAt` |
| `sortDir` | `@RequestParam sortDir` | string | 否 | `desc` | 排序方向，支持 `asc/desc` |
| `Authorization` | 请求头 | string | 是 | - | `Bearer <accessToken>` |

### 1.2 响应壳

统一沿用 `Result<TaskDashboardResponse>`：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `code` | number | 是 | 成功固定为 `200` |
| `errorCode` | string | 否 | 失败时业务错误码 |
| `message` | string | 是 | 响应提示 |
| `data` | object | 是 | 工作台快照 |

### 1.3 响应示例

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "userStats": {
      "runningTaskCount": 2,
      "avgRunningMemoryFitScore": 0.63,
      "queuedTaskCount": 3,
      "maxQueuedAgingScore": 8.50,
      "completedTaskCount": 18,
      "weeklySuccessRate": 92.31
    },
    "taskList": {
      "records": [
        {
          "id": 101,
          "userId": 1,
          "gpuId": null,
          "title": "llama3-train",
          "description": "7B 模型训练",
          "taskType": "model_training",
          "minMemoryGb": 24,
          "status": 2,
          "statusLabel": "Queued",
          "basePriority": 7,
          "estimatedSeconds": null,
          "actualSeconds": null,
          "errorMessage": null,
          "enqueueAt": "2026-04-11T22:00:00",
          "dispatchedAt": null,
          "estimatedFinishAt": null,
          "finishedAt": null,
          "createdAt": "2026-04-11T21:59:50",
          "queuePosition": 1,
          "runningSeconds": null,
          "totalDurationSeconds": null,
          "reviewStatus": null,
          "waitSeconds": 180,
          "resultSummary": null,
          "agingScore": 8.50,
          "progressPct": null,
          "remainingExecutionSeconds": null,
          "memoryFitScore": null
        }
      ],
      "total": 23,
      "size": 10,
      "current": 1,
      "pages": 3
    },
        "gpuRadar": {
          "totalGpuCount": 8,
          "availableGpuCount": 3,
          "overallUtilizationRate": 71.43,
          "systemAvgWaitSeconds": 52.17,
          "systemFreeMemoryGb": 96.00,
      "nextReleaseAt": "2026-04-11T22:45:00",
      "nextReleaseInSeconds": 312
    },
    "priorityTop5": [
      {
        "taskId": 101,
        "displayName": "我的任务 llama3-train",
        "minMemoryGb": 24,
        "agingScore": 8.50,
        "isCurrentUser": true
      },
      {
        "taskId": 102,
        "displayName": "User_A sdxl-train",
        "minMemoryGb": 16,
        "agingScore": 8.20,
        "isCurrentUser": false
      }
    ],
    "timestamp": "2026-04-11T22:39:48"
  }
}
```

### 1.4 页面用途映射

| 页面区域 | 来源字段 |
|---|---|
| 顶部任务统计卡片 | `data.userStats` |
| 我的任务列表 | `data.taskList.records` |
| 列表分页 | `data.taskList.total/current/size/pages` |
| GPU 资源雷达 | `data.gpuRadar` |
| 优先队列 Top5 | `data.priorityTop5` |
| 页面快照时间 | `data.timestamp` |

---

## WebSocket 实时订阅

## 1. 连接方式

| 项 | 值 |
|---|---|
| 端点 | `/ws` |
| 协议 | 原生 WebSocket STOMP / SockJS + STOMP |
| Broker 前缀 | `/topic`、`/queue` |
| Application 前缀 | `/app` |

前端在登录后建立连接，并使用当前登录用户的 `userId` 订阅工作台主题。

原生 WebSocket STOMP 示例：
```ts
import { Client } from "@stomp/stompjs";

const client = new Client({
  brokerURL: "ws://localhost:8080/ws",
});

client.onConnect = () => {
  client.subscribe(`/topic/task-dashboard/${userId}`, (message) => {
    const payload = JSON.parse(message.body);
    console.log(payload);
  });
};

client.activate();
```

SockJS STOMP 示例：
```ts
import { Client } from "@stomp/stompjs";
import SockJS from "sockjs-client";

const client = new Client({
  webSocketFactory: () => new SockJS("http://localhost:8080/ws"),
});

client.onConnect = () => {
  client.subscribe(`/topic/task-dashboard/${userId}`, (message) => {
    const payload = JSON.parse(message.body);
    console.log(payload);
  });
};

client.activate();
```

## 2. 订阅主题

| 主题 | 消息体 | 说明 |
|---|---|---|
| `/topic/task-dashboard/{userId}` | `TaskDashboardResponse` | 当前用户工作台完整快照 |

示例：

```ts
const topic = `/topic/task-dashboard/${userId}`;
```

## 3. 推送策略

- 定时推送：
  - 默认每 `3000ms` 推送一次
- 立即推送：
  - 当前用户任务状态变更后，会额外触发一次该用户工作台快照推送
- 消息体：
  - 与 `GET /api/tasks/dashboard` 的 `data` 结构一致

## 4. 前端处理建议

- 首屏先走一次 `GET /api/tasks/dashboard`
- WebSocket 连接成功后订阅 `/topic/task-dashboard/{userId}`
- 收到消息后直接整体替换工作台状态
- 断线时建议：
  - 保留最后一次快照
  - 自动重连
  - 重连成功后重新调用一次 `GET /api/tasks/dashboard` 做校准

---

## DTO 字段映射

## 1. `TaskDashboardResponse`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `userStats` | `TaskDashboardResponse.userStats` | object | 是 | 当前用户任务统计 |
| `taskList` | `TaskDashboardResponse.taskList` | object | 是 | 当前用户任务分页结果 |
| `gpuRadar` | `TaskDashboardResponse.gpuRadar` | object | 是 | GPU 雷达快照 |
| `priorityTop5` | `TaskDashboardResponse.priorityTop5` | array | 是 | 优先队列 Top5 |
| `timestamp` | `TaskDashboardResponse.timestamp` | string(datetime) | 是 | 本次快照时间 |

## 2. `CurrentUserTaskStats`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `runningTaskCount` | `CurrentUserTaskStats.runningTaskCount` | number | 是 | 运行中任务数 |
| `avgRunningMemoryFitScore` | `CurrentUserTaskStats.avgRunningMemoryFitScore` | number | 是 | 运行中任务平均显存贴合度 |
| `queuedTaskCount` | `CurrentUserTaskStats.queuedTaskCount` | number | 是 | 排队中任务数 |
| `maxQueuedAgingScore` | `CurrentUserTaskStats.maxQueuedAgingScore` | number | 是 | 排队中最高 Aging 得分 |
| `completedTaskCount` | `CurrentUserTaskStats.completedTaskCount` | number | 是 | 已完成任务数 |
| `weeklySuccessRate` | `CurrentUserTaskStats.weeklySuccessRate` | number | 是 | 当前自然周成功率，单位为百分比数值，如 `92.31` |

### 2.1 统计口径

- `avgRunningMemoryFitScore`
  - 公式：`任务所需显存 / 已分配 GPU 总显存`
  - 仅统计 `RUNNING` 任务
- `weeklySuccessRate`
  - 分子：本周 `COMPLETED` 任务数
  - 分母：本周终态任务数 `COMPLETED + FAILED + CANCELLED + REJECTED`

## 3. `TaskWorkbenchPage`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `records` | `TaskWorkbenchPage.records` | `TaskWorkbenchItem[]` | 是 | 当前页列表 |
| `total` | `TaskWorkbenchPage.total` | number | 是 | 总记录数 |
| `size` | `TaskWorkbenchPage.size` | number | 是 | 每页条数 |
| `current` | `TaskWorkbenchPage.current` | number | 是 | 当前页码 |
| `pages` | `TaskWorkbenchPage.pages` | number | 是 | 总页数 |

## 4. `TaskWorkbenchItem`

### 4.1 基础字段

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | `TaskWorkbenchItem.id` | number | 是 | 任务ID |
| `userId` | `TaskWorkbenchItem.userId` | number | 是 | 任务所属用户ID |
| `gpuId` | `TaskWorkbenchItem.gpuId` | number/null | 否 | 分配到的 GPU ID |
| `title` | `TaskWorkbenchItem.title` | string | 是 | 任务名称 |
| `description` | `TaskWorkbenchItem.description` | string | 否 | 任务描述 |
| `taskType` | `TaskWorkbenchItem.taskType` | string | 是 | 任务类型 |
| `minMemoryGb` | `TaskWorkbenchItem.minMemoryGb` | number | 是 | 所需显存（GB） |
| `status` | `TaskWorkbenchItem.status` | number | 是 | 状态码 |
| `statusLabel` | `TaskWorkbenchItem.statusLabel` | string | 是 | 状态文本 |
| `basePriority` | `TaskWorkbenchItem.basePriority` | number | 否 | 基础优先级 |
| `estimatedSeconds` | `TaskWorkbenchItem.estimatedSeconds` | number | 否 | 预估执行时长（秒） |
| `actualSeconds` | `TaskWorkbenchItem.actualSeconds` | number | 否 | 实际执行时长（秒） |
| `errorMessage` | `TaskWorkbenchItem.errorMessage` | string | 否 | 错误或拒绝原因 |
| `enqueueAt` | `TaskWorkbenchItem.enqueueAt` | string(datetime) | 否 | 入队时间 |
| `dispatchedAt` | `TaskWorkbenchItem.dispatchedAt` | string(datetime) | 否 | 开始运行时间 |
| `estimatedFinishAt` | `TaskWorkbenchItem.estimatedFinishAt` | string(datetime) | 否 | 预计完成时间 |
| `finishedAt` | `TaskWorkbenchItem.finishedAt` | string(datetime) | 否 | 完成时间 |
| `createdAt` | `TaskWorkbenchItem.createdAt` | string(datetime) | 否 | 创建时间 |

### 4.2 派生字段

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `queuePosition` | `TaskWorkbenchItem.queuePosition` | number/null | 否 | 排队中的队列位置，从 `1` 开始 |
| `runningSeconds` | `TaskWorkbenchItem.runningSeconds` | number/null | 否 | 运行中的已运行秒数 |
| `totalDurationSeconds` | `TaskWorkbenchItem.totalDurationSeconds` | number/null | 否 | 终态任务总耗时秒数 |
| `reviewStatus` | `TaskWorkbenchItem.reviewStatus` | string/null | 否 | 审核状态展示文案 |
| `waitSeconds` | `TaskWorkbenchItem.waitSeconds` | number/null | 否 | 排队等待秒数 |
| `resultSummary` | `TaskWorkbenchItem.resultSummary` | string/null | 否 | 终态任务结果摘要 |
| `agingScore` | `TaskWorkbenchItem.agingScore` | number/null | 否 | Aging 优先得分 |
| `progressPct` | `TaskWorkbenchItem.progressPct` | number/null | 否 | 运行进度百分比数值，如 `36.25` |
| `remainingExecutionSeconds` | `TaskWorkbenchItem.remainingExecutionSeconds` | number/null | 否 | 剩余执行秒数 |
| `memoryFitScore` | `TaskWorkbenchItem.memoryFitScore` | number/null | 否 | 显存贴合度 |

### 4.3 状态分支说明

#### 排队中 `QUEUED`

- 重点使用：
  - `queuePosition`
  - `waitSeconds`
  - `agingScore`

#### 运行中 `RUNNING`

- 重点使用：
  - `runningSeconds`
  - `progressPct`
  - `remainingExecutionSeconds`
  - `memoryFitScore`

#### 已完成 `COMPLETED`

- 重点使用：
  - `totalDurationSeconds`
  - `resultSummary`
- 成功文案固定为：
  - `任务已归档，可查看运行日志和资源分配记录。`

#### 待审核 `PENDING_APPROVAL`

- 重点使用：
  - `reviewStatus`
- 当前返回：
  - `待审核`

#### 已拒绝 `REJECTED`

- 重点使用：
  - `reviewStatus`
  - `resultSummary`

### 4.4 状态码枚举

| 状态码 | 标签 | 含义 |
|---|---|---|
| `1` | `Pending` | 初始化中 |
| `2` | `Queued` | 排队中 |
| `3` | `Running` | 运行中 |
| `4` | `Completed` | 已完成 |
| `5` | `Failed` | 已失败 |
| `6` | `Cancelled` | 已取消 |
| `7` | `Pending Approval` | 待审核 |
| `8` | `Rejected` | 已拒绝 |

## 5. `GpuRadarSnapshot`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `totalGpuCount` | `GpuRadarSnapshot.totalGpuCount` | number | 是 | 系统 GPU 总数 |
| `availableGpuCount` | `GpuRadarSnapshot.availableGpuCount` | number | 是 | 当前可用 GPU 数，按 `IDLE` 状态统计 |
| `overallUtilizationRate` | `GpuRadarSnapshot.overallUtilizationRate` | number | 是 | 总体显存利用率百分比，如 `71.43` |
| `systemAvgWaitSeconds` | `GpuRadarSnapshot.systemAvgWaitSeconds` | number | 是 | 系统所有排队任务平均等待秒数 |
| `systemFreeMemoryGb` | `GpuRadarSnapshot.systemFreeMemoryGb` | number | 是 | 系统当前总空闲显存（GB） |
| `nextReleaseAt` | `GpuRadarSnapshot.nextReleaseAt` | string(datetime)/null | 否 | 下一次预计释放时间 |
| `nextReleaseInSeconds` | `GpuRadarSnapshot.nextReleaseInSeconds` | number/null | 否 | 距离下一次释放的剩余秒数 |

### 5.1 指标口径

- `overallUtilizationRate`
  - 公式：`(总显存 - 总空闲显存) / 总显存 * 100`
- `availableGpuCount`
  - 当前按 `GpuStatus.IDLE` 统计
- `systemAvgWaitSeconds`
  - 仅统计系统内 `QUEUED` 任务
- `nextReleaseAt`
  - 取所有 `RUNNING` 任务中最早的 `estimatedFinishAt`

## 6. `PriorityQueueTopItem`

| 前端字段 | 后端字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `taskId` | `PriorityQueueTopItem.taskId` | number | 是 | 任务ID |
| `displayName` | `PriorityQueueTopItem.displayName` | string | 是 | 前端直接展示名称 |
| `minMemoryGb` | `PriorityQueueTopItem.minMemoryGb` | number | 是 | 显存需求（GB） |
| `agingScore` | `PriorityQueueTopItem.agingScore` | number | 是 | Aging 得分 |
| `isCurrentUser` | `PriorityQueueTopItem.isCurrentUser` | boolean | 是 | 是否为当前用户任务 |

### 6.1 匿名显示规则

- 当前用户：
  - `我的任务 + 空格 + 任务名称`
- 其他用户：
  - `User_A/User_B/User_C ... + 空格 + 任务名称`
- 同一条响应内：
  - 同一个其他用户会稳定映射到同一个匿名名

---

## 前端对接建议

## 1. 首屏加载流程

1. 从登录态或 `GET /api/auth/me` 获取当前 `userId`
2. 请求 `GET /api/tasks/dashboard`
3. 用 `data` 填充整页状态
4. 建立 `/ws` 连接
5. 订阅 `/topic/task-dashboard/{userId}`

## 2. 实时更新策略

- 推荐策略：
  - `REST 首屏 + WebSocket 全量快照替换`
- 收到实时消息后：
  - 直接整体替换 `userStats/taskList/gpuRadar/priorityTop5/timestamp`
- 不建议：
  - 前端自行基于旧状态做复杂字段重算

## 3. 精度与展示建议

- 百分比字段：
  - `avgRunningMemoryFitScore`
  - `weeklySuccessRate`
  - `overallUtilizationRate`
  - `progressPct`
  - 前端按设计决定是否补 `%`
- 时间字段：
  - `waitSeconds`
  - `runningSeconds`
  - `totalDurationSeconds`
  - `remainingExecutionSeconds`
  - 建议前端格式化为 `xh ym zs`

## 4. 容错建议

- `nextReleaseAt` 和 `nextReleaseInSeconds` 可能为 `null`
- 非对应状态的派生字段会返回 `null`
- 前端应按状态决定展示对应字段，不要假设所有字段都有值

---

## 后端实现依据

- `src/main/java/com/sakurain/gpuscheduler/controller/GpuTaskController.java`
- `src/main/java/com/sakurain/gpuscheduler/service/TaskDashboardService.java`
- `src/main/java/com/sakurain/gpuscheduler/service/TaskDashboardPushService.java`
- `src/main/java/com/sakurain/gpuscheduler/service/TaskNotificationService.java`
- `src/main/java/com/sakurain/gpuscheduler/config/WebSocketConfig.java`
- `src/main/java/com/sakurain/gpuscheduler/dto/task/TaskDashboardResponse.java`
- `src/main/java/com/sakurain/gpuscheduler/dto/task/CurrentUserTaskStats.java`
- `src/main/java/com/sakurain/gpuscheduler/dto/task/TaskWorkbenchPage.java`
- `src/main/java/com/sakurain/gpuscheduler/dto/task/TaskWorkbenchItem.java`
- `src/main/java/com/sakurain/gpuscheduler/dto/task/GpuRadarSnapshot.java`
- `src/main/java/com/sakurain/gpuscheduler/dto/task/PriorityQueueTopItem.java`
